// Code generated by go-api-sdk-apple-dm-codegen. DO NOT EDIT.

package profiles

import (
	"time"
)

// TopLevel — Top Level.
// The top-level payload properties for all profiles.
//
// Supported: iOS 4.0+, macOS 10.7+, tvOS 9.0+, visionOS 1.0+, watchOS 1.0+.
type TopLevel struct {
	// The reverse-DNS style identifier (`com.example.myprofile`, for example) that identifies the
	// profile. The system uses this string to determine whether to replace an existing profile or add
	// it as a new profile.
	PayloadIdentifier string `plist:"PayloadIdentifier" json:"PayloadIdentifier"`
	// The globally unique identifier for the profile. The actual content is unimportant. In macOS, you
	// can use `uuidgen` to generate reasonable UUIDs.
	PayloadUUID string `plist:"PayloadUUID" json:"PayloadUUID"`
	// The type of payload. The only supported value is `Configuration`.
	PayloadType2 string `plist:"PayloadType" json:"PayloadType"`
	// The version number of the profile format, which needs to be `1`. This number represents the
	// version of the configuration profile as a whole, not of the individual profiles within it.
	PayloadVersion int64 `plist:"PayloadVersion" json:"PayloadVersion"`
	// The array of payload dictionaries. If `IsEncrypted` is `true`, this array isn't needed.
	PayloadContent []TopLevelPayloadContentItem `plist:"PayloadContent" json:"PayloadContent"`
	// Enabled if `IsEncrypted` is `true`.
	EncryptedPayloadContent []byte `plist:"EncryptedPayloadContent,omitempty" json:"EncryptedPayloadContent,omitempty"`
	// The description of the profile, shown on the Detail screen for the profile. Make this
	// description detailed enough to help the user decide whether to install the profile.
	PayloadDescription *string `plist:"PayloadDescription,omitempty" json:"PayloadDescription,omitempty"`
	// The human-readable name for the profile, which doesn't need to be unique. The system displays
	// this value on the Detail screen.
	PayloadDisplayName *string `plist:"PayloadDisplayName,omitempty" json:"PayloadDisplayName,omitempty"`
	// The human-readable string that contains the name of the organization that provided the profile.
	PayloadOrganization *string `plist:"PayloadOrganization,omitempty" json:"PayloadOrganization,omitempty"`
	// If present and set to `true`, the user can't delete the profile unless the profile has a removal
	// password and the user provides it.
	//
	// Default: false.
	PayloadRemovalDisallowed *bool `plist:"PayloadRemovalDisallowed,omitempty" json:"PayloadRemovalDisallowed,omitempty"`
	// A string that defines whether to install the profile for the system or the user. In many cases,
	// it determines the location of certificate items, such as keychains. Though it's not possible to
	// declare different payload scopes, payloads like VPN can automatically install their items in
	// both scopes, if needed.
	PayloadScope *TopLevelPayloadScope `plist:"PayloadScope,omitempty" json:"PayloadScope,omitempty"`
	// The date when the system automatically removes the profile.
	RemovalDate *time.Time `plist:"RemovalDate,omitempty" json:"RemovalDate,omitempty"`
	// The number of seconds until the profile is automatically removed. If the `RemovalDate` key is
	// present, the system uses whichever field yields the earliest date.
	DurationUntilRemoval *float64 `plist:"DurationUntilRemoval,omitempty" json:"DurationUntilRemoval,omitempty"`
	// The date when a profile is no longer valid and the system presents an update button to the user.
	PayloadExpirationDate *time.Time `plist:"PayloadExpirationDate,omitempty" json:"PayloadExpirationDate,omitempty"`
	// The type of platform of the target device. Specifying the platform type helps prevent unintended
	// installations.
	//
	// Default: 0.
	TargetDeviceType *TopLevelTargetDeviceType `plist:"TargetDeviceType,omitempty" json:"TargetDeviceType,omitempty"`
	// A dictionary that includes:
	ConsentText *TopLevelConsentText `plist:"ConsentText,omitempty" json:"ConsentText,omitempty"`
}

// TopLevelPayloadContentItem is the PayloadContentItem dictionary.
// The payload-specific content for this profile.
type TopLevelPayloadContentItem struct {
	// A payload item as defined by each payload type.
	ANY any `plist:"ANY" json:"ANY"`
}

// TopLevelConsentTextConsentTextItem is the ConsentTextItem dictionary.
// The dictionary containing a key that consists of the IETF BCP 47 identifier for a language (for
// example, en or jp) and a value that consists of the agreement localized to that language.
type TopLevelConsentTextConsentTextItem struct {
	// The key consisting of the IETF BCP 47 identifier for a language (for example, en or jp) and the
	// value consisting of the agreement localized to that language.
	ANY string `plist:"ANY" json:"ANY"`
}

// TopLevelConsentText is the ConsentText dictionary.
// A dictionary that includes:
type TopLevelConsentText struct {
	// The dictionary containing a key that consists of the IETF BCP 47 identifier for a language (for
	// example, en or jp) and a value that consists of the agreement localized to that language.
	ConsentTextItem TopLevelConsentTextConsentTextItem `plist:"ConsentTextItem" json:"ConsentTextItem"`
}
