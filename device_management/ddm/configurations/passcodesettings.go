// Code generated by go-api-sdk-apple-dm-codegen. DO NOT EDIT.

package configurations

// PasscodeSettings — Passcode:Settings.
// The declaration to configure passcode policy settings.
//
// Supported: iOS 15.0+, macOS 13.0+, visionOS 2.0+, watchOS 10.0+.
type PasscodeSettings struct {
	// If `true`, the system requires the user to set a passcode without any requirements about the
	// length or quality of the passcode. The presence of any other keys implicitly requires a
	// passcode, and overrides this key's value.
	//
	// Default: false.
	RequirePasscode *bool `plist:"RequirePasscode,omitempty" json:"RequirePasscode,omitempty"`
	// If `true`, the passcode needs to consist of at least one alphabetic character and at least one
	// number.
	//
	// Default: false.
	RequireAlphanumericPasscode *bool `plist:"RequireAlphanumericPasscode,omitempty" json:"RequireAlphanumericPasscode,omitempty"`
	// If `true`, the system requires a complex passcode. A complex passcode is one that doesn't
	// contain repeated characters or increasing or decreasing characters (such as 123 or CBA).
	//
	// Default: false.
	RequireComplexPasscode *bool `plist:"RequireComplexPasscode,omitempty" json:"RequireComplexPasscode,omitempty"`
	// The minimum number of characters a passcode can contain.
	//
	// Default: 0.
	MinimumLength *int64 `plist:"MinimumLength,omitempty" json:"MinimumLength,omitempty"`
	// Specifies the minimum number of complex characters in the password. A complex character is a
	// character other than a number or a letter, such as `&`, `%`, `$`, and `#`.
	//
	// Default: 0.
	MinimumComplexCharacters *int64 `plist:"MinimumComplexCharacters,omitempty" json:"MinimumComplexCharacters,omitempty"`
	// The number of failed passcode attempts that the system allows the user before it erases or locks
	// the device. After six failed attempts, the device imposes a time delay before the user can enter
	// a passcode again. The time delay increases with each failed attempt. On macOS, set
	// `FailedAttemptsResetInMinutes` to define the time delay. The time delay begins after the sixth
	// attempt, so if `MaximumFailedAttempts` is six or lower, the system has no time delay and
	// triggers the erase or lock as soon as the user exceeds the limit.
	//
	// Default: 11.
	MaximumFailedAttempts *int64 `plist:"MaximumFailedAttempts,omitempty" json:"MaximumFailedAttempts,omitempty"`
	// The number of minutes before the login is reset after the maximum number of failed attempts.
	// Also set the `MaximumFailedAttempts` key for this to take effect.
	FailedAttemptsResetInMinutes *int64 `plist:"FailedAttemptsResetInMinutes,omitempty" json:"FailedAttemptsResetInMinutes,omitempty"`
	// The maximum period that a user can select, during which the user can unlock the device without a
	// passcode. A value of `0` means no grace period, and the device requires a passcode immediately.
	// In the absence of this key, the user can select any period. In macOS, the system translates this
	// to screensaver settings.
	MaximumGracePeriodInMinutes *int64 `plist:"MaximumGracePeriodInMinutes,omitempty" json:"MaximumGracePeriodInMinutes,omitempty"`
	// The maximum period that a user can select, during which the device can be idle before the system
	// automatically locks it. When the device reaches this limit, the device locks and the user must
	// enter the passcode to unlock it. In the absence of this key, the user can select any period. In
	// macOS, the system translates this to screensaver settings.
	MaximumInactivityInMinutes *int64 `plist:"MaximumInactivityInMinutes,omitempty" json:"MaximumInactivityInMinutes,omitempty"`
	// Specifies the maximum number of days that the passcode can remain unchanged. After this number
	// of days, the system forces the user to change the passcode before it unlocks the device.
	MaximumPasscodeAgeInDays *int64 `plist:"MaximumPasscodeAgeInDays,omitempty" json:"MaximumPasscodeAgeInDays,omitempty"`
	// The number of historical passcode entries the system checks when validating a new passcode. The
	// device refuses a new passcode if it matches a previously used passcode within the specified
	// passcode history range. In the absence of this key, the system performs no historical check.
	PasscodeReuseLimit *int64 `plist:"PasscodeReuseLimit,omitempty" json:"PasscodeReuseLimit,omitempty"`
	// If `true`, the system forces a password reset the next time the user tries to authenticate. If
	// you set this key in a configuration in the system scope (device channel), the setting takes
	// effect for all users, and admin authentication may fail until the admin user password is also
	// reset.
	//
	// Default: false.
	ChangeAtNextAuth *bool `plist:"ChangeAtNextAuth,omitempty" json:"ChangeAtNextAuth,omitempty"`
	// Specifies a regular expression, and its description, to enforce password compliance. Use the
	// simpler passcode settings whenever possible, and rely on regular expression matching only when
	// necessary. Mistakes in regular expressions can lead to frustrating user experiences, such as
	// unsatisfiable passcode policies, or policy descriptions that don't match the enforced policy.
	CustomRegex *PasscodeSettingsCustomRegex `plist:"CustomRegex,omitempty" json:"CustomRegex,omitempty"`
}

// PasscodeSettingsCustomRegexDescription is the Description dictionary.
// A dictionary with supported OS language IDs for the keys (such as `en-US`), and values that
// represent a localized description of the policy that the regular expression enforces. Use the
// special `default` key for languages that the dictionary doesn't contain.
type PasscodeSettingsCustomRegexDescription struct {
	// A localized description.
	ANY *string `plist:"ANY,omitempty" json:"ANY,omitempty"`
}

// PasscodeSettingsCustomRegex is the CustomRegex dictionary.
// Specifies a regular expression, and its description, to enforce password compliance. Use the
// simpler passcode settings whenever possible, and rely on regular expression matching only when
// necessary. Mistakes in regular expressions can lead to frustrating user experiences, such as
// unsatisfiable passcode policies, or policy descriptions that don't match the enforced policy.
type PasscodeSettingsCustomRegex struct {
	// A regular expression string to match against the password to determine whether it complies with
	// a policy. The regular expression uses the ICU syntax. The string can't exceed 2048 characters in
	// length.
	Regex string `plist:"Regex" json:"Regex"`
	// A dictionary with supported OS language IDs for the keys (such as `en-US`), and values that
	// represent a localized description of the policy that the regular expression enforces. Use the
	// special `default` key for languages that the dictionary doesn't contain.
	Description *PasscodeSettingsCustomRegexDescription `plist:"Description,omitempty" json:"Description,omitempty"`
}
