// Code generated by go-api-sdk-apple-dm-codegen. DO NOT EDIT.

package assets

// Acme — ACME Credential.
// An ACME identity that the device generates.
//
// Supported: iOS 17.0+, macOS 14.0+, tvOS 17.0+, visionOS 1.1+, watchOS 10.0+.
type Acme struct {
	// Specifies the directory URL of the ACME server. Use the `https` scheme for the URL.
	DirectoryURL string `plist:"DirectoryURL" json:"DirectoryURL"`
	// The server can use this as a one-time code to prevent issuing multiple certificates. It also
	// indicates to the ACME server that the device has access to a valid client identifier that the
	// enterprise infrastructure issued. This can help the ACME server determine whether to trust the
	// device, however this is a relatively weak indication because of the risk that an attacker may
	// intercept and duplicate the client identifier.
	ClientIdentifier string `plist:"ClientIdentifier" json:"ClientIdentifier"`
	// The valid values for `KeySize` depend on the values of `KeyType` and `HardwareBound`. See those
	// keys for specific requirements.
	KeySize int64 `plist:"KeySize" json:"KeySize"`
	// Specifies the type of key pair to generate.
	KeyType AcmeKeyType `plist:"KeyType" json:"KeyType"`
	// If `false`, the private key isn't bound to the device.
	HardwareBound bool `plist:"HardwareBound" json:"HardwareBound"`
	// The device requests this subject for the certificate that the ACME server issues. The ACME
	// server may override or ignore this field in the certificate it issues.
	Subject [][][]string `plist:"Subject" json:"Subject"`
	// Specifies the subject's alternative name that the device requests for the certificate that the
	// ACME server issues. The ACME server may override or ignore this field in the certificate it
	// issues.
	SubjectAltName *AcmeSubjectAltName `plist:"SubjectAltName,omitempty" json:"SubjectAltName,omitempty"`
	// The device requests this key usage for the certificate that the ACME server issues. The ACME
	// server may override or ignore this field in the certificate it issues.
	UsageFlags *int64 `plist:"UsageFlags,omitempty" json:"UsageFlags,omitempty"`
	// The device requests this extended key usage for the certificate that the ACME server issues. The
	// ACME server may override or ignore this field in the certificate it issues.
	ExtendedKeyUsage []string `plist:"ExtendedKeyUsage,omitempty" json:"ExtendedKeyUsage,omitempty"`
	// If `true`, the device provides attestations that describe the device and the generated key to
	// the ACME server. The server can use the attestations as strong evidence that the key is bound to
	// the device, and that the device has properties listed in the attestation. The server can use
	// that as part of a trust score to decide whether to issue the requested certificate. When
	// `Attest` is `true`, set `HardwareBound` to `true`. On macOS, set this key, if present, to
	// `false`. The hardware requirements for attestation are described below.
	//
	// Default: false.
	Attest *bool `plist:"Attest,omitempty" json:"Attest,omitempty"`
}

// AcmeSubjectAltName is the SubjectAltName dictionary.
// Specifies the subject's alternative name that the device requests for the certificate that the
// ACME server issues. The ACME server may override or ignore this field in the certificate it
// issues.
type AcmeSubjectAltName struct {
	// The RFC 822 email address.
	Rfc822Name *string `plist:"rfc822Name,omitempty" json:"rfc822Name,omitempty"`
	// The DNS name.
	DNSName *string `plist:"dNSName,omitempty" json:"dNSName,omitempty"`
	// The uniform resource identifier.
	UniformResourceIdentifier *string `plist:"uniformResourceIdentifier,omitempty" json:"uniformResourceIdentifier,omitempty"`
	// The NT principal name. Use an other name OID set to `1.3.6.1.4.1.311.20.2.3`.
	NtPrincipalName *string `plist:"ntPrincipalName,omitempty" json:"ntPrincipalName,omitempty"`
}
